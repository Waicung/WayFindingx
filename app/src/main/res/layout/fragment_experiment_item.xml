<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="New Text"
        android:id="@+id/instruction_textView"
        android:layout_alignParentTop="true"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:textSize="@dimen/abc_text_size_headline_material"
        android:paddingTop="@dimen/abc_action_bar_icon_vertical_padding_material"
        android:paddingBottom="@dimen/abc_action_bar_icon_vertical_padding_material" />

    <RelativeLayout
        android:id="@+id/panel"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/instruction_textView">
        <Button
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:id="@+id/achieve_button"
            android:drawableLeft="@drawable/ic_achieve_sm"
            android:text="Arrived"
            android:layout_alignParentTop="true"
            android:layout_alignParentRight="true"
            android:layout_alignParentEnd="true"
            style="@style/Base.Widget.AppCompat.Button.Colored" />

        <Button
            android:text=" Feedback"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:id="@+id/feedback_button"
            android:drawableLeft="@drawable/ic_feedback_sm"
            android:layout_alignParentTop="true"
            android:layout_toLeftOf="@+id/achieve_button"
            android:layout_toStartOf="@+id/achieve_button"
            android:layout_marginRight="30dp"
            android:layout_marginEnd="30dp"
            style="@style/Base.Widget.AppCompat.Button.Colored" />
    </RelativeLayout>
</RelativeLayout>
